<% if @error == true %>
<div class="container mx-auto mt-1">
  <div class="alert alert-danger mx-auto text-center " style= "width: 30rem;" role="alert">
    <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
    <ul style="list-style: none;">
      <% @gossip.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
</div>
<% end %>

<div class="container text-center mt-5 mb-5" style="width: 30rem;">

  <div class="container mx-auto text-center">
    <h4>Ajoute un gossip à la liste et devient le nouveau maître du monde ! </h4>
    <hr>
    <form action= <%= gossips_path %> method= "POST">
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
      <div class="form-group">
        <label>Titre du gossip</label>
        <input type="text" name="gossip_title" class="form-control">
      </div>
      <div class="form-group">
        <label>Ton super gossip</label>
        <input type="text" name= "gossip_content" class="form-control" >
      </div>
      <button type="submit" class="btn btn-primary">Créer</button>
    </form>

  </div>
</div>
