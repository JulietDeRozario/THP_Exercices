<div class="container text-center mt-5 mb-5" style="width: 20rem;">
  <h3>Éditer le commentaire</h3>
  <hr>
  <%= form_tag gossip_comment_path(params[:gossip_id], params[:id]), method: "put" do %>
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">

  <div class = "form-group">
    <%= label_tag 'Commentaire' %>
    <%= text_field_tag 'content', @comment.content, class:"form-control" %>
  </div>

  <%= submit_tag "Commenter", class:"btn btn-primary"%>

  <% end %>

</div>
